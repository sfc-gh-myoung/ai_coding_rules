schema_version: 3
updated: 2026-05-17T16:26:51-04:00
id: complex-cortex-search-build
description: # TODO
variant: complex
prompt: |
  We're building a customer facing search experience backed by a cortex search
  service. The semantic view we'll expose to end users sits on top of three
  curated tables. Walk me through both pieces end to end, from the search
  service definition to how the semantic model surfaces measures and
  dimensions to a chat interface.
expected:
  required:
    - rules/000-global-core.md  # foundation (always loaded)
    - rules/106c-snowflake-semantic-views-integration.md  # suggested: kw: semantic model, semantic view
    - rules/116-snowflake-cortex-search.md  # suggested: kw: cortex search, search
    - rules/106-snowflake-semantic-views-core.md  # migrated from dependencies (v3.15)
  forbidden: []
  optional:
    - rules/100-snowflake-core.md  # auto-demoted (was dep): no trigger evidence
      # missing: ext=.sql
trigger_evidence:
  kw: [cortex search, search, semantic model, semantic view]   # suggested
    # n-gram (no rule): "customer facing search experience backed"
    # n-gram (no rule): "through both pieces end"
    # n-gram (no rule): "a customer facing search experience"
    # n-gram (no rule): "both pieces end to"
    # n-gram (no rule): "building a customer facing search"
  ext: []
  file: []
  dir: []
