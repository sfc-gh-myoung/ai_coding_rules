schema_version: 3
updated: 2026-07-04T00:00:00Z
id: complex-cortex-search-build
description: # TODO
variant: complex
prompt: |
  We're building a customer-facing search service backed by cortex-search.
  The semantic view we'll expose to end users sits on top of three
  curated tables. Walk me through both pieces end to end, from the search
  service definition to how the semantic model surfaces measures and
  dimensions to a chat interface.
expected:
  required:
    - rules/000-global-core.md  # foundation (always loaded)
    - rules/116-snowflake-cortex-search.md  # matches: kw:cortex-search, kw:search service
    - rules/106-snowflake-semantic-views-core.md  # matches: kw:tables (semantic-view/model kws are hyphenated post-C1)
  forbidden: []
  optional:
    - rules/106c-snowflake-semantic-views-integration.md  # optional; hyphenated kws do not literal-match prompt
    - rules/100-snowflake-core.md  # auto-demoted (was dep): no trigger evidence
      # missing: ext=.sql
trigger_evidence:
  kw: [cortex-search, search service, tables, semantic model]
    # cortex-search matches rule 116 kw:cortex-search
    # search service matches rule 116 kw:search service (appears twice in prompt)
    # tables matches rule 106 kw:tables
    # semantic model is prompt-only annotation (rule 106 has kw:semantic-model hyphenated)
  ext: []
  file: []
  dir: []
