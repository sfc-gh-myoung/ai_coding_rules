schema_version: 3
updated: 2026-07-04T00:00:00Z
id: complex-cortex-search-build
description: # TODO
variant: complex
prompt: |
  We're building a customer-facing cortex search service with search tool configuration.
  We'll also CREATE SEMANTIC VIEW on top of three curated TABLES PRIMARY KEY entities.
  Walk me through both pieces end to end, from the search service definition to how
  the semantic model surfaces FACTS DIMENSIONS METRICS to a chat interface.
expected:
  required:
    - rules/000-global-core.md  # foundation (always loaded)
    - rules/116-snowflake-cortex-search.md  # matches: kw:cortex-search, kw:search service
    - rules/106-snowflake-semantic-views-core.md  # matches: kw:tables (semantic-view/model kws are hyphenated post-C1)
  forbidden: []
  optional:
    - rules/106c-snowflake-semantic-views-integration.md  # optional; hyphenated kws do not literal-match prompt
    - rules/100-snowflake-core.md  # auto-demoted (was dep): no trigger evidence
      # missing: ext=.sql
trigger_evidence:
  kw: [cortex search service, search tool configuration, CREATE SEMANTIC VIEW, TABLES PRIMARY KEY, FACTS DIMENSIONS METRICS]
  ext: []
  file: []
  dir: []
