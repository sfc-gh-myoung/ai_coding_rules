schema_version: 3
updated: 2026-05-17T16:26:51-04:00
id: complex-python-etl
description: # TODO
variant: complex
prompt: |
  Our nightly ETL is implemented in jobs/extract_load.py. It currently uses a
  fragile retry loop and we have observed intermittent timeouts when the source
  database is under load. Please review the file and suggest a more resilient
  retry policy with jittered backoff, and add structured logging so we can
  trace each attempt. Do not change the public function signature; downstream
  orchestration depends on it.
expected:
  required:
    - rules/000-global-core.md  # foundation (always loaded)
    - rules/207-python-logging.md  # suggested: kw: logging
    - rules/200-python-core.md  # migrated from dependencies (v3.15)
  forbidden: []
  optional: []
trigger_evidence:
  kw: [logging]   # suggested
    # n-gram (no rule): "change the public function"
    # n-gram (no rule): "database is under load"
    # n-gram (no rule): "fragile retry loop and"
    # n-gram (no rule): "have observed intermittent timeouts"
    # n-gram (no rule): "nightly ETL is implemented"
  ext: [.py]   # suggested
  file: []
  dir: []
