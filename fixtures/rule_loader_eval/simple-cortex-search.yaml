schema_version: 3
updated: 2026-05-17T16:26:51-04:00
id: simple-cortex-search
description: # TODO
variant: simple
prompt: |
  Set up a cortex search service over our document corpus.
expected:
  required:
    - rules/000-global-core.md  # foundation (always loaded)
    - rules/116-snowflake-cortex-search.md  # suggested: kw: cortex search, search
  forbidden: []
  optional:
    - rules/100-snowflake-core.md  # auto-demoted (was dep): no trigger evidence
      # missing: ext=.sql
trigger_evidence:
  kw: [cortex search, search]   # suggested
    # n-gram (no rule): "a cortex search service over"
    # n-gram (no rule): "cortex search service over our"
    # n-gram (no rule): "over our document corpus"
    # n-gram (no rule): "service over our document"
    # n-gram (no rule): "up a cortex search service"
  ext: []
  file: []
  dir: []
