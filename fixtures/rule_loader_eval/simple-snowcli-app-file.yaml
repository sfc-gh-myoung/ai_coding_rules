schema_version: 3
updated: 2026-05-17T16:26:51-04:00
id: simple-snowcli-app-file
description: # TODO
variant: simple
prompt: |
  Add an additional native app artifact entry to snowflake.yml.
expected:
  required:
    - rules/000-global-core.md  # foundation (always loaded)
    - rules/112-snowflake-snowcli.md  # suggested: file: snowflake.yml
  forbidden: []
  optional:
    - rules/100-snowflake-core.md  # auto-demoted (was dep): no trigger evidence
      # missing: ext=.sql
trigger_evidence:
  kw: []
    # n-gram (no rule): "additional native app artifact"
    # n-gram (no rule): "native app artifact entry"
    # n-gram (no rule): "an additional native app"
    # n-gram (no rule): "app artifact entry to"
    # n-gram (no rule): "artifact entry to snowflake"
  ext: []
  file: [snowflake.yml]   # suggested
  dir: []
