schema_version: 3
updated: 2026-07-04T00:00:00Z
id: simple-warehouse
description: # TODO
variant: simple
prompt: |
  Create a warehouse configuration for our analytics team, sized for
  interactive dashboards.
expected:
  required:
    - rules/000-global-core.md  # foundation (always loaded)
    - rules/119-snowflake-warehouse-management.md  # matches: kw:warehouse configuration, kw:size warehouse
  forbidden: []
  optional:
    - rules/100-snowflake-core.md  # auto-demoted (was dep): no trigger evidence
      # missing: ext=.sql
trigger_evidence:
  kw: [warehouse configuration]
    # warehouse configuration matches rule 119 kw:warehouse configuration
  ext: []
  file: []
  dir: []
